services:
  - type: web
    name: pedrra-training
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        sync: false
